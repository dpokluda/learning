winget install python3

# PyTorch core: tensors, autograd, neural network layers, and the training primitives (optimizers, loss functions).
pip install torch 

# Companion library for computer vision: ready-to-use datasets (including MNIST), image transforms, and pretrained vision models.
pip install torchvision 

# Standard Python plotting library; you'll use it to visualize sample digits, loss curves, and misclassified examples.
pip install matplotlib

# Extras:

# Fundamental array library; PyTorch interoperates with it and many examples/snippets assume it's available.
pip install numpy
# Interactive notebook environment
pip install jupyter
# Tiny progress-bar library; makes training loops show a live progress bar instead of silent waits.
pip install tqdm
